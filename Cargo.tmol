[package]
name = "weave_lang"
version = "0.1.1"  # Bumped for updates
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ndarray = "0.15"
rand = "0.8"

[dev-dependencies]
# No additional dev dependencies needed yet

[features]
default = []
gradient = ["ndarray"]  # Use ndarray for gradient computations
parser = []             # Placeholder for future nom integration
